{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bundle": "webpack",
    "build": "node ../index -m x64 -m nosign -m release -i ./build/native-test.js -o ./out.exe",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "heapdump": "^0.3.7"
  },
  "devDependencies": {
    "ignore-loader": "^0.1.1",
    "webpack": "^1.13.3",
    "xbin-loader": "^1.0.1"
  }
}
